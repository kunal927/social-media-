<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Post</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background-color: #f0f2f5;
      font-family: 'Segoe UI', sans-serif;
    }
    .post-card {
      max-width: 600px;
      margin: 60px auto;
      background: #fff;
      padding: 25px;
      border-radius: 20px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
      transition: all 0.3s ease-in-out;
    }
    .post-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 30px rgba(0,0,0,0.15);
    }
    .profile-img {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid #0d6efd;
    }
    textarea {
      border: none;
      resize: none;
      font-size: 1rem;
      outline: none;
      padding: 10px;
      width: 100%;
      background: #f8f9fa;
      border-radius: 12px;
    }
    textarea:focus {
      background: #fff;
      border: 1px solid #0d6efd;
      box-shadow: 0 0 8px rgba(13,110,253,0.3);
    }
    .btn-post {
      border-radius: 25px;
      padding: 8px 20px;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .btn-post i {
      font-size: 1.2rem;
    }
    .icons-bar {
      display: flex;
      gap: 18px;
      font-size: 1.3rem;
      color: #6c757d;
      margin-top: 10px;
    }
    .icons-bar i:hover {
      color: #0d6efd;
      cursor: pointer;
    }
    .name {
      text-transform: uppercase;
       margin-left: 20px ;
       font-size: 17px;
    }
  </style>
</head>
<body>

  <div class="post-card">
    <form action="/createpost" method="POST" enctype="multipart/form-data">
      <div class="d-flex align-items-center mb-3">
        <% if(profile && profile.profileImage) { %>
          <img src="/uploads/<%= profile.profileImage %>" alt="Profile" class="profile-img">
        <% } else { %>
          <img src="https://via.placeholder.com/50?text=User" alt="Profile" class="profile-img">
        <% } %>
      
        <h5 class="mb-0 fw-bold  name"><%=user.FullName%></h5>
      </div>

      <div class="mb-3">
        <textarea name="content" rows="9" placeholder="What's happening?" required></textarea>
      </div>

      <!-- Icons bar like Twitter -->
      <div class="icons-bar mb-3">
        <i class="bi bi-image" title="Add Image"></i>
        <i class="bi bi-camera-video" title="Add Video"></i>
        <i class="bi bi-emoji-smile" title="Add Emoji"></i>
        <i class="bi bi-geo-alt" title="Add Location"></i>
        <i class="bi bi-hash" title="Add Hashtag"></i>
      </div>

      <div class="text-end">
        <button type="submit" class="btn btn-primary btn-post">
          <i class="bi bi-send-fill"></i> Post
        </button>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
