<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About - MitroNet</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #f8fafc;
      margin: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    /* Navbar */
    .top-nav {
      width: 100%;
      height: 65px;
      background: #fff;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 25px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.08);
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    .premium-logo {
      font-size: 1.9rem;
      font-weight: 700;
      letter-spacing: 1px;
    }
    .mitro { color: #ff6b6b; }
    .net { color: #1e90ff; }

    .nav-icons {
      display: flex;
      align-items: center;
      gap: 28px;
    }
    .nav-icons i, .nav-icons a i {
      font-size: 1.5rem;
      cursor: pointer;
      color: #495057;
      transition: color 0.2s ease, transform 0.2s ease;
    }
    .nav-icons i:hover { color: #0d6efd; transform: scale(1.1); }
    .nav-icons .profile-img {
      width: 46px;
      height: 46px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid #1e90ff;
    }

    /* Main layout */
    .main {
      display: flex;
      flex: 1;
    }

    /* Sidebar */
    .sidebar {
      width: 240px;
      background: #fff;
      padding: 25px 20px;
      border-right: 1px solid #e6e6e6;
      flex-shrink: 0;
      min-height: calc(100vh - 65px);
      box-shadow: 2px 0 8px rgba(0,0,0,0.05);
    }
    .sidebar a {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px;
      margin-bottom: 12px;
      color: #050505;
      font-weight: 500;
      text-decoration: none;
      border-radius: 10px;
      transition: background 0.2s, transform 0.2s;
    }
    .sidebar a:hover {
      background: #f1f5ff;
      transform: translateX(5px);
    }
    .sidebar i { font-size: 1.2rem; color: #1877f2; }
    .sidebar .profile-img {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid #ff6b6b;
    }

    /* Content */
    .content {
      flex: 1;
      padding: 40px;
    }
    .about-card {
      background: #fff;
      padding: 30px;
      border-radius: 18px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.08);
      max-width: 800px;
      margin: auto;
      text-align: center;
    }
    .about-card h2 {
      color: #1e90ff;
      font-weight: 700;
      margin-bottom: 15px;
    }
    .about-card p {
      color: #555;
      font-size: 1.05rem;
      line-height: 1.6;
    }

    /* Footer */
    footer {
      background: #fff;
      padding: 20px;
      text-align: center;
      border-top: 1px solid #ddd;
      font-size: 0.9rem;
      color: #777;
    }

    @media(max-width: 768px){
      .main { flex-direction: column; }
      .sidebar { width: 100%; display: flex; justify-content: space-around; border-right: none; border-bottom: 1px solid #ddd; padding: 15px 0; }
      .sidebar a { margin-bottom: 0; padding: 10px; }
      .content { padding: 20px; }
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <div class="top-nav">
    <div class="premium-logo"><span class="mitro">Mitro</span><span class="net">Net</span></div>
    <div class="nav-icons">
      <i class="fa-solid fa-house"></i>
      <a href="/createpost"><i class="fa-solid fa-pen-to-square"></i></a>
      <a href="/about"><i class="fa-solid fa-diagram-project"></i></a>
      <i class="fa-solid fa-gamepad"></i>
     
    </div>
  </div>

  <!-- Main layout -->
  <div class="main">
    <!-- Sidebar -->
    <div class="sidebar">
    
      <a href="/createpost"><i class="fa-solid fa-plus"></i> Create Post</a>
      <a href="/connections"><i class="fa-solid fa-users"></i> Connections</a>
      <a href="/dashboard"><i class="fa-solid fa-chart-simple"></i> Dashboard</a>
      <a href="/logout"><i class="fa-solid fa-right-from-bracket"></i> Logout</a>
    </div>

    <!-- Content -->
    <div class="content">
      <div class="about-card">
        <h2>About MitroNet</h2>
        <p>
          Welcome to <strong>MitroNet</strong>, a premium social platform where you can share updates, 
          connect with friends, and discover new ideas.  
          Every user can post, engage, and explore content seamlessly.  
          With MitroNet, your network of friends and connections stays just one click away.
        </p>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    &copy; <%= new Date().getFullYear() %> MitroNet. All rights reserved.
  </footer>

</body>
</html>
