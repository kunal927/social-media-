<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= user.FullName %> - Profile</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body style="font-family: 'Poppins', sans-serif; background: linear-gradient(135deg,#dfe9f3,#ffffff);">

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card profile-card shadow-lg border-0 rounded-4 p-4">
          <div class="card-body text-center">
            
            <!-- Profile Image -->
            <div class="profile-img-wrapper mb-4">
        <img src="<%= profile.profileImage ? '/uploads/' + profile.profileImage : 'https://via.placeholder.com/180?text=User' %>" 
          alt="Profile Photo" 
          class="rounded-circle border border-4 shadow-lg profile-img"
          style="width: 180px; height: 180px; object-fit: cover;">
            </div>

            <!-- Name + Headline -->
            <h2 class="fw-bold text-dark mb-1"><%= user.FullName %></h2>
            <h5 class="text-muted mb-4"><%= profile.headline || "No headline added" %></h5>

            <!-- Info Grid -->
            <div class="row g-3 text-start">
              <div class="col-md-6">
                <div class="info-box p-3 rounded">
                  <p><strong>Education:</strong> <%= profile.education || "Not specified" %></p>
                  <p><strong>Location:</strong> <%= profile.location || "Not specified" %></p>
                  <p><strong>City:</strong> <%= profile.city || "Not specified" %></p>
                </div>
              </div>
              <div class="col-md-6">
                <div class="info-box p-3 rounded">
                  <p><strong>Date of Birth:</strong> <%= profile.dob || "Not specified" %></p>
                  <p><strong>Contact Info:</strong> <%= profile.contact || "Not specified" %></p>
                </div>
              </div>
            </div>

            <!-- Buttons -->
            <div class="mt-4">
              <a href="/editprofile" class="btn btn-gradient me-2">✏️ Edit Profile</a>
              <a href="/loginSuccessful" class="btn btn-outline-dark">⬅ Go Back</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    /* Card Styling */
    .profile-card {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .profile-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 40px rgba(0,0,0,0.15);
    }

    /* Profile Image */
    .profile-img-wrapper {
      position: relative;
      display: inline-block;
    }
    .profile-img {
      transition: 0.3s ease;
      border: 4px solid #0d6efd;
    }
    .profile-img:hover {
      box-shadow: 0 0 30px #0d6efd, 0 0 15px #fff;
      transform: scale(1.05);
    }

    /* Info Box */
    .info-box {
      background: #f8f9fa;
      border: 1px solid #e9ecef;
      transition: 0.3s ease;
    }
    .info-box:hover {
      background: #eaf2ff;
      border-color: #0d6efd;
      box-shadow: 0 4px 12px rgba(13,110,253,0.15);
    }
    .info-box p {
      margin: 0 0 8px;
      font-size: 0.95rem;
    }

    /* Gradient Button */
    .btn-gradient {
      background: linear-gradient(45deg, #0d6efd, #6610f2);
      border: none;
      color: #fff;
      font-weight: 600;
      padding: 10px 20px;
      border-radius: 30px;
      transition: all 0.3s ease;
    }
    .btn-gradient:hover {
      background: linear-gradient(45deg, #6610f2, #0d6efd);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }

  </style>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
