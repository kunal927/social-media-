<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Logout</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: rgba(0,0,0,0.05);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .logout-modal {
      background: #fff;
      width: 100%;
      max-width: 400px;
      border-radius: 20px;
      padding: 30px 25px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.2);
      text-align: center;
      transition: all 0.3s ease;
    }

    .logout-modal:hover {
      transform: translateY(-3px);
      box-shadow: 0 25px 60px rgba(0,0,0,0.25);
    }

    .logout-modal h3 {
      font-weight: 600;
      margin-bottom: 25px;
      color: #14171a;
    }

    .btn-logout {
      width: 100%;
      border-radius: 9999px;
      font-weight: 600;
      padding: 10px;
      margin-bottom: 12px;
      transition: all 0.3s ease;
    }

    .btn-logout-danger {
      background-color: #e0245e;
      border: none;
      color: #fff;
    }

    .btn-logout-danger:hover {
      background-color: #c81e52;
    }

    .btn-logout-secondary {
      background-color: #f5f8fa;
      border: none;
      color: #14171a;
    }

    .btn-logout-secondary:hover {
      background-color: #e1e8ed;
    }

    .fa-door-open {
      margin-right: 8px;
    }

    @media(max-width: 500px){
      .logout-modal {
        padding: 25px 15px;
      }
    }
  </style>
</head>
<body>

  <div class="logout-modal">
    <i class="fas fa-door-open fa-2x mb-3" style="color:#e0245e;"></i>
    <h3>Log out of your account?</h3>
    <form action="/logout" method="POST">
      <button type="submit" class="btn btn-logout btn-logout-danger">
        <i class="fas fa-sign-out-alt"></i> Log out
      </button>
    </form>
    <a href="/dashboard" class="btn btn-logout btn-logout-secondary">
      Cancel
    </a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
