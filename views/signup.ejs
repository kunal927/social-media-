<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: #f0f2f5;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .signup-container {
      display: flex;
      flex-direction: row;
      background: #fff;
      border-radius: 25px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.15);
      overflow: hidden;
      max-width: 900px;
      width: 100%;
    }

    /* Left gradient panel */
    .signup-left {
      background: linear-gradient(135deg, #ff7f50 0%, #1e90ff 100%);
      color: #fff;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 50px 40px;
      text-align: center;
    }

    .signup-left h1 {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 15px;
    }

    .signup-left p {
      font-size: 1.2rem;
      line-height: 1.5;
    }

    /* Right form panel */
    .signup-right {
      flex: 1;
      padding: 50px 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      position: relative;
    }

    .signup-right h2 {
      text-align: center;
      margin-bottom: 30px;
      color: #111;
      font-weight: 700;
    }

    .form-control {
      border-radius: 12px;
      padding: 12px 15px;
      font-size: 0.95rem;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      transition: all 0.3s ease;
    }

    .form-control:focus {
      border-color: #ff7f50;
      box-shadow: 0 0 8px rgba(255,127,80,0.3);
      outline: none;
    }

    .btn-primary {
      background: #ff7f50;
      border: none;
      border-radius: 25px;
      padding: 10px 0;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      background: #e76b3c;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    }

    .btn-outline-primary {
      border-radius: 25px;
      padding: 10px 0;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
    }

    .btn-outline-primary:hover {
      background: #1e90ff;
      color: #fff;
      border-color: #1e90ff;
    }

    .alert {
      border-radius: 15px;
      padding: 15px 20px;
    }

    .signup-footer {
      text-align: center;
      margin-top: 20px;
      font-size: 0.85rem;
      color: #6c757d;
    }

    /* Premium logo style */
    .premium-logo {
      font-size: 2rem;
      font-weight: 700;
      font-family: 'Segoe UI', sans-serif;
      letter-spacing: 1px;
      padding: 8px 25px;
      border-radius: 12px;
      background: linear-gradient(135deg, rgba(255,255,255,0.2), rgba(255,255,255,0.05));
      display: inline-block;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: default;
      margin-bottom: 25px;
    }

    .premium-logo:hover {
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(0,0,0,0.15);
    }

    .mitro {
      color: #ff6b6b;
    }

    .net {
      color: #1e90ff;
    }

    @media(max-width: 768px){
      .signup-container {
        flex-direction: column;
        border-radius: 20px;
      }
      .signup-left {
        display: none;
      }
      .signup-right {
        padding: 40px 25px;
      }
    }
  </style>
</head>
<body>

  <div class="signup-container">
    <!-- Left info panel -->
    <div class="signup-left">
      <div class="premium-logo">
        <span class="mitro">Mitro</span><span class="net">Net</span>
      </div>
      <p>Connect with friends and the world around you. Share, like, and comment!</p>
    </div>

    <!-- Right form panel -->
    <div class="signup-right">
      <h2>Signup</h2>

      <% if (Array.isArray(errors) && errors.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <strong>Error!</strong>
          <ul class="mb-0">
            <% errors.forEach(function(error) { %>
              <li><%= error %></li>
            <% }) %>
          </ul>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% } %>

      <form action="/signup" method="POST">
        <input type="text" name="FullName" class="form-control" placeholder="Full Name" required value="<%= oldInput.FullName ? oldInput.FullName : '' %>">
        <input type="email" name="Email" class="form-control" placeholder="Email" required value="<%= oldInput.Email ? oldInput.Email : '' %>">
        <input type="password" name="Password" class="form-control" placeholder="Password" required value="<%= oldInput.Password ? oldInput.Password : '' %>">
        <button type="submit" class="btn btn-primary w-100 mb-3">Signup</button>
      </form>

      <div class="text-center">
        <span>Already have an account?</span>
        <a href="/login" class="btn btn-outline-primary w-100 mt-2">Login</a>
      </div>

      <div class="signup-footer mt-3">
        &copy; <%= new Date().getFullYear() %> Mitronet
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
